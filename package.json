{
  "name": "@scola/api-router",
  "version": "2.0.0",
  "description": "API Router",
  "repository": "github:scola84/node-api-router",
  "author": "M.C. van den Boogaart <scola@kvdb.name>",
  "license": "MIT",
  "main": "./dist/api-router.js",
  "jsnext:main": "index.js",
  "dependencies": {
    "@scola/error": "^4.1.0",
    "async": "^2.1.2",
    "path-to-regexp": "^1.7.0",
    "semver": "^5.3.0"
  },
  "devDependencies": {
    "rollup": "^0.36.3",
    "rollup-plugin-buble": "^0.14.0",
    "uglify-js": "^2.7.4"
  },
  "scripts": {
    "build": "npm run cleanup && npm run rollup",
    "build:min": "npm run build && npm run min",
    "cleanup": "rm -rf dist",
    "min": "uglifyjs dist/api-router.js -m -o dist/api-router.min.js",
    "prepublish": "npm run build:min",
    "rollup": "rollup -c"
  }
}
